---
interface Props {
	backUrl?: string;
}

const { backUrl } = Astro.props;

const url = Astro.url.toString();
const idx = (url.endsWith("/") ? url.slice(0, -1) : url).lastIndexOf("/");
const oneLevelUpUrl = url.slice(0, idx + 1);
---

<button class="*:opacity-60 font-mono my-4">
	<span> {">"}</span>
	<a href={backUrl ?? oneLevelUpUrl} class="hover:opacity-100"> cd .. </a>
</button>
