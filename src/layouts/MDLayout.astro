---
import Layout from "./Layout.astro";

interface Props {
	frontmatter: {
		title: string;
	};
}
const {
	frontmatter: { title },
} = Astro.props;
---

<Layout title={title}>
	<article class="prose m-auto text-dark/75 dark:text-light/75">
		<slot />
	</article>
</Layout>
